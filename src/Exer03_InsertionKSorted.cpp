//
// Created by Junio Moreira on 2026-08-26.
//

#include <iostream>
#include <vector>

using namespace std;

using ll = long long;

#define FAST_IO ios_base::sync_with_stdio(false); cin.tie(NULL);

// TODO: Sort a k-sorted array using Insertion Sort and count total inner comparisons
ll insertion_sort_count_comparisons(vector<int>& arr) {
    ll comp_count = 0;
    // Implementation goes here
    return comp_count;
}

int main() {
    FAST_IO;

    // TODO: Read N, k and array, sort using insertion_sort_count_comparisons,
    // print sorted array on the first line and total comparisons on the second line

    return 0;
}